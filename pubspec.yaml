name: media_watch_tasks
description: Detect new local photos on Android/iOS via periodic scan + state diff, then enqueue upload tasks for reliable background processing.
version: 0.1.0
homepage: https://example.com
repository: https://example.com

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter
  background_fetch: ^1.3.7
  crypto: ^3.0.3
  drift: ^2.18.0
  meta: ^1.12.0
  path: ^1.9.0
  path_provider: ^2.1.4
  photo_manager: ^3.1.1
  sqlite3_flutter_libs: ^0.5.24
  uuid: ^4.4.2

dev_dependencies:
  drift_dev: ^2.18.0
  lints: ^3.0.0
  build_runner: ^2.4.12

flutter:
  uses-material-design: false


